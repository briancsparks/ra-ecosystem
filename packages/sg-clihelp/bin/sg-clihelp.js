#!/usr/bin/env node

const msg = `
# sg-clihelp

Includes the following:

* on the root 'sg' objectg
  * sg0
  * sg-argv
  * sg-exec
* On their respcecive sub-objects
  * sg.fs     === fs
  * sg.path   === path
  * sg.os     === 0s
  * sg.util   === util
  * sg.sh     === shelljs
  * sg.ARGV   === sg.ARGV
* Other
  * sg.die
  * sg.grepLines
  * sg.include
  * sg.from
  * sg.startupDone
  * sg.runTopAsync

`;

// console.log(msg);
